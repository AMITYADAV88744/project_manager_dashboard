import 'package:flutter/cupertino.dart';

class PersonalDeatil extends StatefulWidget {
  const PersonalDeatil({Key? key}) : super(key: key);

  @override
  _PersonalDeatilState createState() => _PersonalDeatilState();
}

class _PersonalDeatilState extends State<PersonalDeatil> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          ListView(
          )
        ],
      )
    );
  }
}
